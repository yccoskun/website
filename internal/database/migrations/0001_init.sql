-- 0001_init: bootstrap migration.
-- The schema_migrations table itself is created by the migrations runner.
-- Real domain tables (posts, resume entries, sessions, ...) arrive in later phases.
SELECT 1;
