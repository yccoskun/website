# Studio media

Studio stills are managed in Admin → Media (upload) and Admin → Studio
(catalogue entries). Files live under `data/uploads/` on the server and are
served at `/media/{id}`.

Do not commit image binaries to this folder.
